#!usr/bin/env python

from setuptools import setup,find_packages

setup(
    name="games.jong",
    version="0.1.0",
    packages = find_packages() ,
    install_requires=[],
    extras_require={
        "develop": []
    },
    entry_points={
    }
)